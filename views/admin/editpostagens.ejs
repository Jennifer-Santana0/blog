<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>editando postagens</title>
</head>
<body>
    <%- include ('../partials/header.ejs') %>
    <%- include ("../partials/msg.ejs") %>

    <div id="container">
        <h4>Editando postagem</h4>
        <form action="/admin/postagem/edit" method="post">
            <input type="hidden" value="<%=postagem._id%>" name="id">
            <label for="titulo">Titulo</label>
            <input type="text" name="titulo" value="<%=postagem.titulo%>">
            <label for="slug">Slug</label>
            <input type="text" name="slug" value="<%=postagem.slug%>">
            <label for="descricao">Descricao</label>
            <input type="text" name="descricao" value="<%=postagem.descricao%>">
            <label for="conteudo">Conteudo</label>
            <textarea name="conteudo"cols="30" rows="10"><%=postagem.conteudo%>
            </textarea>
            <select name="categoria">
                <%if(categoria.length>0){%>
                    <%for(let i = 0;i<categoria.length;i++){%>
                        <option value="<%=categoria[i].id%>"><%=categoria[i].nome%></option>
                    <%}%>
                <%} else {%>
                    <option value="">Nenhuma categoriaa listada</option>
                <%}%>
               
            </select>
            <button type="submit">Editar postagens</button>
        </form>
        
    </div>

</body>
</html>